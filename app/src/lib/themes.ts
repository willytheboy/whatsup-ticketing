import type { ThemeId } from "./features";

/** The browser-chrome colour per theme (matches --g2 in globals.css). */
export const THEME_COLOR: Record<ThemeId, string> = { cedar: "#639922", volt: "#84CC16", sunset: "#14B8A6", ultraviolet: "#8B5CF6" };
